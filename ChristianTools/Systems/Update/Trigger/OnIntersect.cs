namespace ChristianTools.Systems.Update
{
    /*public partial class Trigger
    {
        public delegate void DxOnIntersectAction();
        public static void OnIntersect(IEntity entity, ITrigger trigger, DxOnIntersectAction OnIntersectAction)
        {
            if (trigger.isActive && entity.isActive)
            {
                if (entity.rigidbody.GetRectangle.Intersects(trigger.rigidbody.GetRectangle))
                {
                    OnIntersectAction();
                }
            }
        }
    }*/
}